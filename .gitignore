# Scratch transcripts / media (regenerated by fetch.py; safe to delete)
work/
demo/_demo_vault/
*.mp3
*.m4a
*.wav
*.part
*.ytdl
*.meta.json

# Persistent capture-dedup ledger — machine-local state, not committed
state/archive.txt
archive*.txt

# Local config (copy of config.example.json)
config.json

# Python / uv
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Logs now live in ~/Library/Logs/youtube-knowledge/ (outside the repo)
